{
  "name": "two-key",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mock payer with a SQLite ledger. Approval bound to an exact payload hash + server-minted operation id.",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "better-sqlite3": "^11.3.0",
    "zod": "^4.6.5"
  }
}
